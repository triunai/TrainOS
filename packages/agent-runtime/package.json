{
  "name": "@trainos/agent-runtime",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Provider-agnostic (BYOK) agent runtime. Runs a real orchestrator against any plugged-in LLM key, executes every tool call through a mock adapter over fixture data, and emits traces in the contract's AutomationRun shape.",
  "exports": {
    ".": "./src/index.ts",
    "./node": "./src/node.ts"
  },
  "types": "./src/index.ts",
  "files": [
    "src",
    "README.md"
  ],
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "cli": "tsx src/cli.ts"
  },
  "dependencies": {
    "@trainos/contract": "*",
    "@trainos/fixtures": "*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22",
    "tsx": "^4.19.2",
    "typescript": "^5",
    "vitest": "^2"
  }
}
