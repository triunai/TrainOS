/**
 * The contacts directory's path, derived from the navigation tree's one rule
 * (`shared/config/nav.ts`): a child of `Sales` lives under `/sales/`.
 */

export const CONTACTS_DIRECTORY_PATH = "/sales/contacts";
