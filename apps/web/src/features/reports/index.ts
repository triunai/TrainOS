/**
 * The reports feature's ONLY public surface (CLAUDE.md R5).
 *
 * One screen and the path it mounts at. `/reports` is the nav tree's only
 * childless parent, so there is no list-and-detail pair here to export.
 */

export { ReportsScreen } from "./ReportsScreen";
export { REPORTS_PATH } from "./paths";
