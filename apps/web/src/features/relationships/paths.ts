/**
 * The relationships paths, derived from the navigation tree's one rule
 * (`shared/config/nav.ts`): a child of `Relationships` lives under
 * `/relationships/`.
 */

export const RENEWALS_PATH = "/relationships/renewals";

export const CROSS_SELL_PATH = "/relationships/cross-sell";

export const MARKETING_PATH = "/relationships/marketing";
