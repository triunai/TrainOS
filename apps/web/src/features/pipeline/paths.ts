/**
 * The pipeline board's path, derived from the navigation tree's one rule
 * (`shared/config/nav.ts`): a child of `Sales` lives under `/sales/`.
 */

export const PIPELINE_BOARD_PATH = "/sales/pipeline";
