# Resume brief — 13 Sep 2026 13:50 (+08) · UI paused, API phase begins

Read this first in the new session, then `supabase/HANDOFF.md`, then `docs/reviews/2026-09-13-verification.md`, then `docs/bd/ai-explorations/README.md`. The previous brief (11:50) is superseded; its blast A–E is complete.

## State at wrap

- **UI is paused by the user's decision.** All 45 nav leaves render real screens (26 with artboards, 30 built today without one, under CLAUDE.md and `docs/design/2026-09-13-design-tightening-brief.md` §1–§19). Every screen has Loading/Error/Empty states, render tests, and light+dark screenshots at 1440×900.
- **Kit additions today:** blue accent RecordHeader (14 record pages), ListToolbar, SplitWorkspace, KanbanBoard, CalendarGrid/List, PartialDataBanner, RowActionMenu, ProfileModal `headerAction`, FilterSelect/FilterSearch, DataTable `variant:"code"` + `rowSuggested`, on-accent danger button variant, content-sized segments (64px floor). `SPLIT_HEADER_HEIGHT` deleted. `--primary-deep/--primary-lift` deleted. Gradient B (violet edge) default, A at `?gradient=alt`.
- **Contract:** 16 commits; all six deferred review gaps closed, five `status: string` fields → enums, `Quotation.status`, `FloorPriceBreachDetails` floors, `AlternativeCategoryRate.ratePerMessageExact`, `RoutingEntry.staged {tier, reason}`, `UsageDailySeries`, `MeProfile` on `GET /v1/me/profile`, `PipelineStage.terminal/outcome` (required), `MessageChannel` exported, `OPPORTUNITY_STAGE_CHANGE` (R18), `certificateIssuedAt` (R17). Twelve enums + two action types pending migration, listed in `supabase/HANDOFF.md`.
- **Fixtures:** Alex Selvarajah is `/me` for MD; 136 participants across 10 cohorts; 78 certificates; one below-floor margin (Safety Leadership, 29%); 14-day usage series; ORG-0114 has an in-flight deal (ENG-0259) with a pending Approval stage.
- **Supabase:** packs 001–011 written with tests and rollbacks, executed on the local PostgreSQL 17 shim, **applied nowhere**. 011 (action envelope, 3,640 lines) committed at wrap with repaired pins. 012–016 per HANDOFF (events/outbox, AI ops, RLS policies + grants, realtime/cron, seed/provisioning), 017 = baseline amendment (revoke PUBLIC execute on nine core fns, jsonb CHECKs, two numeric precisions). **Every table is deny-all until 014 lands.** Codex is quota-blocked until 17:10 on 13 Sep; ruling: Claude drafts, Codex (gpt-5.6-sol xhigh) reviews. Kimi: see HANDOFF for whether a review path exists.
- **Backend write-up for a first-time reviewer:** artifact https://claude.ai/code/artifact/4225b80f-92f8-4073-8d19-9b341fa86a33 (source in the previous session's scratchpad; regenerate from `supabase/migrations`, `packages/contract`, `packages/agent-runtime` if needed).
- **Dev server:** `npx vite --port 5180` from `apps/web`. Demo `/dev/demo`, kit `/dev/kit`.

## Rulings pending the user (do not act unprompted)

1. Theme switch moved into the ProfileModal header to stop the name truncating beside the new collapse control; user may want it back beside the name (then the collapse control moves to the footer).
2. Participants list: eight-segment status track (806px) + two facets cannot share one row at 1440 by 148px; stacked with a DEV-only warning banner. Options: narrower track, hide zero-count segments, or accept.
3. Cobalt removal (keep only #1F5BFF) — deferred, see memory.
4. Participants stacking aside, the §10a segment floor is 64px; §10's 120px is withdrawn.

## Shared-worktree rules (they cost us six sweeps today; all in memory too)

- Commit ONLY `git add -- <files> && git commit -m "..." -- <files>`. Never a bare `git commit`. Never amend/reset/rebase.
- A pathspec commit records the WORKING TREE of the named file, not your hunks: before committing any shared file (`kit/index.ts`, `tokens.css`, `tailwind.config.ts`, `FixtureClient.ts`, `HANDOFF.md`, the brief, any barrel) run `git diff --cached --numstat -- <file>` and confirm the line count is yours; after, `git show HEAD -- <file>`.
- One named owner per shared file per blast. New files must be `git add`ed first.
- A barrel line can reach HEAD before its module: run `npm run check:barrels` (added by the verifier; if absent, `git ls-files` vs barrel exports) before any cut.
- Tests: root `npm test -- --run` or from `apps/web`; never bare `vitest` at root. Rerun once on phantom failures while other lanes write.
- Screenshots: own browser context per lane; reject frames under 20KB; pin the viewport (another lane resized the shared one to 1496 today).
- `lefthook.yml` runs `lint-staged --no-stash` (a stash restore lost work once).
- **Prefer worktrees next blast** for lanes with disjoint folders and no kit additions; the shared index is the one thing they fix.

## WRAP 13 Sep 19:05 (+08) — START HERE. A network outage at ~16:00 killed the local migrations lane mid-014 and the first cloud migrations lane; nothing of 014–017 landed and no dirty SQL was left. Since then: PR #1 routing MERGED, PR #2 worker MERGED (main c3d12b8, typecheck clean in a clean worktree). Relaunched at 19:03 in the cloud, each with a PR to main: `cloud/migrations` (014–017 from a fresh shim, PR "feat(supabase): migrations 014–017"), `cloud/web-swap` (enquiries → proposals → approvals through the TrainOsClient seam), `cloud/pack-v3` (proposal pack rewrite). `cloud/opus-pass` MERGED (PR #3, dea0c32); `cloud/pack-v3` MERGED (PR #4, 083cc47). Merge order when they land: web-swap → migrations. Still gated on the user: 018 RPC pack (L2), first hosted apply (L3; needs `core` exposed in the dashboard and a DB secret in env), region confirmation, n8n in the proposal.

## BLAST 13 Sep 19:25 (+08) — Orchestrator launched four lanes on top of the 19:05 recovery, alongside the still-running `cloud/migrations` (014–017) and `cloud/web-swap` (enquiries → proposals → approvals through the TrainOsClient seam), neither with a PR yet. New, each in its own worktree under `~/Repos/personal-work/trainos-wt/` opening a PR to main: `lane/rpc-018` (worktree `trainos-wt/rpc-018`, the 018 RPC pack); `ui/tokens` (worktree `trainos-wt/ui-tokens`, kit contrast tokens + mono-uppercase reduction); `ui/lists` (worktree `trainos-wt/ui-lists`, HRD Corp + Invoices list leaves, Collections §10b, zebra on two hand-rolled tables); `ui/states` (worktree `trainos-wt/ui-states`, nine empty states, ten tone ternaries, Drawer primary scope, ListToolbar on agent registry). User's 19:23 rulings: go for 018; go for hosted apply after 014 passes retrofit QA (hosted project `balzmmsmrawzmefkavte`, ap-southeast-1, ACTIVE_HEALTHY, zero migrations applied, reachable via the Supabase MCP so L3 uses `apply_migration` rather than psql); region Singapore confirmed. Still open for the user: `core` exposed in the dashboard (R-F), n8n in the proposal, the four UI rulings above. Results land as PRs to main; `spine-keeper` records each lane's result in the doc spine as it commits, one commit per update.

## BLAST 13 Sep 19:4x (+08) — Verified against the repo, not taken on trust: PR #5 (`cloud/web-swap`, "refactor(web): enquiries, proposals and approvals through the TrainOsClient seam") and PR #6 (`cloud/migrations`, "feat(supabase): migrations 014–017") are both open. **PR #5's CI is not "green except Gitleaks"** — `gh pr checks 5` on run 34754549631 shows four failures: Gitleaks (org repo needs a `GITLEAKS_LICENSE` secret the `gitleaks-action@v2` refuses to run without), Prettier drift check (real formatting drift in `apps/web/scripts/check-barrels.mjs`), npm audit high+ (8 real vulnerabilities, 5 moderate/1 high/2 critical, in the `vite`/`vite-node` and `react-router`/`react-router-dom` chains — `npm audit fix --force` would force a breaking `react-router-dom@7.18.3`), and Vite build (the build step passes; only `actions/upload-artifact@v4` fails, on "Artifact storage quota has been hit", a GitHub Actions account limit, not a code defect). Merge order stays web-swap → migrations. `ci-gitleaks` (Sonnet, worktree `trainos-wt/ci-gitleaks`, branch `ci/gitleaks`) is replacing the action with the pinned binary for the license gate specifically. PR #5 also diffed in ten `RPC_NAMES` entries with no SQL behind them (`patch_enquiry_extraction`, `list_follow_ups`, `get_follow_up_draft`, `list_proposals`, `add_proposal_section`, `put_proposal_section`, `regenerate_proposal_section`, `list_quotations`, `get_rate_card`, `get_audit`) — confirmed by diffing the PR; `lane/rpc-018` has been told to implement all ten in 018, marking "spec derived from client" where `docs/architecture/09` has no spec. New lane `seeds` (Opus, worktree `trainos-wt/seeds`, branch `lane/seeds`, shim port 5434): `supabase/seeds` fixture world + wipe + pin, per the user's 19:27 goal stated directly as "write seeds too for test purposes." New lane `codex-review-011-013` (Codex gpt-5.6-sol xhigh via `codex-rescue`, worktree `trainos-wt/codex-011-013`, branch `review/codex-011-013`): D-012 adversarial review of packs 011–013 to `docs/reviews/2026-09-13-codex-retrofit-011-013.md`; fallback order if Codex is unavailable is Kimi (not installed locally, effectively skipped) then a second Opus reviewer. Codex quota was reported back at a 19:29 probe (not independently re-verified here). **Repo name:** GitHub is `PARALLELPARADIGMS/alex-project` — "trainos" is only the local directory name and the `@trainos/*` package scope; do not search GitHub for "trainos". Also confirmed: branch protection on `main` cannot be set at all right now — `gh api repos/.../branches/main/protection` returns 403 "Upgrade to GitHub Pro or make this repository public," so that decision needs the user before it can even be attempted.

## BLAST 13 Sep 19:5x (+08) — Verified against the repo: main itself has been red since `9fdcb4d` (run 34753909066) on the same four checks PR #5 fails — Gitleaks (license), Prettier (drift), Vite build (artifact-upload quota, build itself passes) and npm audit high+ (8 vulnerabilities, 2 critical). PR #5, #6 (`cloud/migrations`, "feat(supabase): migrations 014–017") and #7 (`ci/gitleaks`, "ci: run gitleaks binary directly, no license needed") all inherit this — confirmed PR #7's Gitleaks check now passes (`gh pr checks 7`, run 34754819325) but the other three main-red failures remain on it too, so fixing Gitleaks alone will not turn any PR green. A lane was reportedly redirected to fix main's CI on branch `fix/main-ci` then merge main into `cloud/web-swap`; the actual worktree (`trainos-wt/fix-pr5`) is on branch `fix/pr5` with a web-swap feature commit (`refactor(web): route the approval screens through the seam`) as its tip, not a CI fix — resume by checking that worktree's `git log` before assuming CI repair is in progress. PR #6 also fails Grant Hygiene: `supabase/tests/test_014_rls_policies_and_client_grants.sql:513` defines its own `SECURITY DEFINER` function, which the guard flags as a test creating the privilege escalation it exists to check for; `codex-review-014-017` (Codex gpt-5.6-sol xhigh, shim :5435, worktree `trainos-wt/codex-014-017` detached at the review commit) must explain or fix this and report MERGE / MERGE-WITH-FIXES / BLOCK to `docs/reviews/2026-09-13-codex-retrofit-014-017.md` before PR #6 merges. **R-F confirmed live, not just reported:** probed the hosted project's REST endpoint directly with the Supabase MCP's publishable key and `Accept-Profile: core`, got back exactly `PGRST106` — "Only the following schemas are exposed: public, graphql_public." Exposing `core` is a real, still-open user action; nothing else currently blocks the hosted apply once 014 clears review.

## BLAST 13 Sep 20:0x (+08) — Verified: PR #7 (`ci/gitleaks`) merged at `0910b9d` (confirmed ancestor of main), the `ci-gitleaks` worktree is shut down, and Gitleaks now passes on main's own CI (run 34755129255) — but Prettier drift, the Vite artifact-storage quota and npm audit high+ still fail there, so main is not green yet. `fix-pr5` (worktree `trainos-wt/fix-pr5`, still branch `fix/pr5` — the `fix/main-ci` name from the last report doesn't match anything on disk, still unexplained) has since committed three real fixes confirmed via its own `git log`: `0b7221e` formats `check-barrels.mjs` for Prettier, `6c84ca0` pins the test suite's timezone, `c4b8a78` stops the shared artifact-storage quota from failing the build/test gates — no PR opened from it yet. The `codex-014-017` worktree flagged in an earlier entry is confirmed legitimate, not a stray: it's `codex-review-014-017`'s own detached-HEAD checkout of PR #6's tip (`826bb52`) for its retrofit review, nothing more. PR #8 (`ui/states`, "fix(screens): empty states, tone ternaries, drawer primary and the registry toolbar") opened ~19:49, reported 1389 tests (28 new) and local gates green (diff shape checked: 23 files, 5 test files — consistent, not independently re-run), under review by `review-pr8` (Sonnet `code-reviewer`) before merge; as of this check its CI is still mostly pending. Three deviations from the verification doc confirmed against the actual files: (1) §6's claim that `statusTone.ts` already had all ten tone-ternary maps was false — four new maps were appended (`AGENT_TONE`, `SEVERITY_TONE`, `MESSAGE_CATEGORY_TONE`, `HOURS_SAVED_TONE`), plus a fifth, feature-local `PARTICIPANT_ATTENDANCE_TONE`; (2) §6's kit-level Drawer primary scope was deferred — `KnowledgeSourcesScreen` migrated to the `ProviderKeysScreen` SECONDARY-action pattern instead, confirmed via the file's own comment; (3) a real defect in `TnaDetailPage` (a DANGER constraint rendering neutral) was fixed, confirmed via the file's own comment and its new `SEVERITY_TONE[constraint.severity]` read. Carried to other lanes rather than dropped: `EnquiryDetailPage`, `CostingWorksheetPage`, `QuotationsListPage` and three M03 `ListToolbar` screens to `cloud/web-swap`; `ClaimPacketScreen` and `CollectionsQueueScreen` to `ui/lists`.

## BLAST 13 Sep 20:1x (+08) — Verified: PR #9 (`ui/tokens`) is already MERGED at `ed3c337` (`gh pr view 9`: state MERGED, mergedAt 11:54:52Z), not "open under review" — it landed between being reported and being checked. Every substantive claim about it held up exactly against `docs/reviews/2026-09-13-verification.md` read at that commit: rows 1b (muted text on a selected row, now 6.54:1 light / 6.69:1 dark via a `SELECTED_TINT` rebind of `--ink-muted`) and 1c (`--on-primary` on solid primary, now 4.82:1 dark via new `--primary-solid`/`--primary-solid-hover` tokens carrying `#1F5BFF` in both themes) both closed; contrast suite exactly "49 cases, was 33"; mono-caps counts exact on all four cited routes (`/dashboard` 29→11, `/sales/enquiries` 34→21, `/compliance/hrd-corp` 22→12, `/finance/invoices` 36→22); the `/training/participants` "x275" confirmed a false violation — 350 of 354 are `PAR-…`/`CERT-…` record references §1 keeps in mono deliberately. Two unfixed defects confirmed real and out of scope: `--on-primary` on `--danger` at 2.22:1 dark (alias in `tailwind.config.ts`, call site in `shared/components/ui/toast.tsx`, outside this lane), and `shared/components/ui/button.tsx` with zero importers anywhere (confirmed via `git grep`) — a dead second button vocabulary. Minor discrepancy: reported 33 files, actual diff has 35; not otherwise material. PR #10 (`ui/lists`) confirmed open, 5 commits and 17 files exact, "1009 passing, 110 files" confirmed verbatim in its own PR body. All six deviations confirmed against the diff: invoice totals in a `<dl>` under the table (kit `DataTable` has no footer); the AI tier matrix losing its sticky first column and staged-row `bg-ai-tint` on conversion (no kit prop for either, confirmed via the diff's own "TWO THINGS THE CONVERSION COSTS" comment); `tsconfig.strict.json` gaining exactly three paths; Collections' `ListToolbar` sitting above the detail pane rather than the table in the master/detail grid (kept, per the PR body's own reasoning); `HRDC_RULE_CHANGES_PATH` confirmed to still carry the same leaf-opens-a-record defect its sibling `HRDC_PACKET_PATH` had before this lane fixed that one — confirmed via the file's own updated comment marking it "NOT this pass's." **One correction to the queued kit follow-ups:** item (a), `MoneyText` hardcoding `font-mono` at `Money.tsx:32,43`, is already fixed — by PR #9 itself (`5f01e57`), confirmed by reading current `Money.tsx` on main (both lines now `tabular-nums`, no `font-mono` anywhere), and independently corroborated by PR #10's own body. Drop it from the follow-up list rather than assigning it to a future lane; items (b)–(f) remain open as reported.

## BLAST 13 Sep 20:2x (+08) — Verified: PR #8 (`ui/states`) confirmed MERGED at `a4ea833` (`gh pr view 8`: mergedAt 11:55:41Z) after `review-pr8`'s MERGE verdict, run in an isolated worktree pinned to `6dfd281` — typecheck clean, 1004 tests (a different figure from the lane's own self-reported 1389, not reconciled) and a recount of 8 replaced tone ternaries not 10 (matches what this brief already had). `ui-states` worktree confirmed shut down. Two of the three UI carry-over PRs (#8, #9) are on main now; only #10 remains under review. **Main-red correction: it was five failing checks on `9fdcb4d`, not four** — re-checking run 34753909066 job-by-job found Vitest (unit) also failed, with five wall-clock/timezone-dependent test failures (`DateText`, `ClientProposalPage`, `knowledge`, `AttendanceCapturePage` ×2), none from PR #5's own diff; missed in the first pass through this run. All three `fix-pr5` fixes are now confirmed as commits directly in PR #5's own history on `cloud/web-swap` (not a separate branch that merges in later) — `git show origin/cloud/web-swap:apps/web/vitest.config.ts` confirms `TZ: "Asia/Kuala_Lumpur"` is pinned, and `.github/workflows/ci.yml` confirms `continue-on-error: true` on the `upload-artifact` step with a comment explaining the account-level quota. Confirmed on PR #5's own latest CI run: Gitleaks, Prettier and Vite build now pass; Vitest was still running at last check. **npm audit ruling only partly implemented**: the reported plan (scope to `npm audit --omit=dev --audit-level=high`, comment the three GHSA ids, land a separate `chore(toolchain): vite 7 + vitest 3` draft PR) is not what's live — `cloud/web-swap` currently has plain `continue-on-error: true` on the unchanged audit command, no `--omit=dev`, no id comment, and no toolchain PR exists yet on GitHub. Recorded as a gap between the stated plan and the current state, not a false report — the plan may simply not be finished yet. **A correction to this session's own prior entry**: the MoneyText follow-up (a) was recorded as fully closed; it is not. `MoneyText` itself is fixed (PR #9's `5f01e57`), but `InvoiceDetailScreen.tsx` hardcodes `font-mono` at ten call sites in its own hand-rolled table (confirmed at `a4ea833`, closes once PR #10's `DataTable` conversion merges) and `ExecutiveDashboard.tsx` hardcodes it at three call sites (confirmed directly, lines 91/349/385) with no lane currently assigned — that one stays open as a screen-level item, verification doc §5 item 5 / §7 row 9, separate from the kit follow-up list.

## BLAST 13 Sep 20:3x (+08) — Verified: PR #6 confirmed with 4 commits (`gh pr view 6`) — 014 RLS/grants/envelope wrappers, 015 cron schedules, 016 tenant provisioning, 017 baseline amendment. Assertions per pack reported as 014×51, 015×13, 016×15, 017×39; 17/17 pins pass from a clean shim including a full reverse rollback to zero relations (not independently re-run, needs the shim). Real defects found and fixed, confirmed in the diff: `app.require_tenant_id` was ungranted so RLS policies errored instead of denying ("no policy in 001-013 used it — 014 is the first to grant it"); `rule_set_versions` lacked its tenant index; `budget_status`/`model_tier_status` views were ungrantable, deferred to 018; `SET LOCAL` refused inside a non-volatile function; the `ADD CONSTRAINT IF NOT EXISTS` trap hit a third time. Shim caveat: `pg_cron`/`pg_net` are stubs, so 015's job registration is pinned but firing is unverified until hosted. Deferred: pipeline seed → 018; tax/HRD rows PROPOSED pending a Finance verifier; four retention reapers await approved policy rows; four AI-ops columns await a runtime vocabulary. Codex second pass requested on 015–017 and nineteen edits to earlier pins. **018 is now PR #11** (`feat(supabase): 018 golden-path RPC pack`, branch `lane/rpc-018`), confirmed open — 3 commits, 3 files (migration/rollback/test, the repo's standard shape), 4955 additions. 23 of 24 `RPC_NAMES` implemented; `me_profile` (the 24th) confirmed NOT implemented, per the migration's own comment: needs an HR table for eleven required fields. One view, nine `app.*` helpers, 160 pin assertions + 28 in-migration verify; `check:rpc` 0 BROKEN, `check:grants` OK, `lint:sql` 42/42 (reported, not re-run). Shim rebuilt from `initdb` on port 5433 with a hand-written platform shim. Confirmed by grep: `core.tax_policies`/`app.resolve_tax_policy` genuinely absent from 001–013, so R-C is resolved by verification, not by need — `Quotation` carries no tax field. A real keyset-cursor bug was found and fixed: a DESCENDING sort took the array's max sort key for the next cursor, the wrong end of the page under DESC, so page two repeated page one's tail — caught by the pack's own pin. `SET CONSTRAINTS IMMEDIATE` fires nothing inside a PL/pgSQL subtransaction, replaced by an explicit read-back. `DEAL_CHAIN` confirmed a genuine, deliberately-unpapered contract/004 divergence (004's CHECK allows `ENGAGEMENT | OPPORTUNITY | PACKET`; the contract wants `DEAL_CHAIN` instead of `PACKET`) — pinned to fail loudly if the divergence is ever silently resolved rather than deliberately reconciled. Doc 09's `search_path` pin asserts a string PostgreSQL never stores — this is [[postgres-search-path-footgun]] confirmed again. **Hard rule, confirmed baked directly into 018's own test as a runtime assertion**: every `core` table is FORCE RLS with zero policies until 014 lands; on a non-BYPASSRLS owner every 018 read then returns zero rows silently, and the test raises "018 MUST NOT be applied to a hosted project before 014" if it ever detects this — the local shim's superuser role is why 018's own pins pass regardless. Follow-up slice requested for `lane/rpc-018`: PR #5's ten new RPC names, rebase on `cloud/migrations`, and the pipeline stage seed.

## BLAST 13 Sep 20:4x (+08) — **Correction to this brief's own 20:3x entry: "014-017 all landed" was premature.** PR #12 (`docs(reviews): D-012 dual adversarial review — PR #6, migration 014 (BLOCK)`) merged at `02240e6` with VERDICT BLOCK. Read `docs/reviews/2026-09-13-codex-retrofit-014-017.md` in full before touching PR #6. Two findings, confirmed directly: (1) at review time PR #6 contained ONLY migration 014 — 015/016/017 did not exist anywhere in the repo (the review's own `git log --all` search across every branch found nothing); they exist in PR #6's current diff now but have never been reviewed by anyone. (2) Migration 014 itself has two CRITICAL findings, confirmed directly in the SQL: `014:625` grants DELETE on `public.memberships` (002 never did), reopening a role-escalation path — an aal2 ADMIN can DELETE their own membership row and INSERT a higher-role replacement, since the self-edit guard is UPDATE-only; and `014`'s rollback runs a blanket `REVOKE ALL ... FROM authenticated, anon, PUBLIC` that strips migration 002's ORIGINAL grants instead of restoring pre-014 state, with a post-condition that asserts the broken state as correct so it can't fail loudly. Five more HIGH/MED findings in the full doc (blanket tenant-scoped SELECT on all 114 `core` tables with no role/permission check, exposing `ai_provider_keys`/`public_share_tokens`/`run_node_io`; a client omission making the approval hash check a silent no-op; wrong policy/relation counts in the header). G6 execution passed (14/14 migrations, 14/14 pins, rollback, re-apply, re-pin all clean) but the review states this explicitly does NOT clear the BLOCK, since no pin exercises the actual defects. The mandated "thermonuclear" reviewer skill doesn't exist in this environment; a substitute adversarial pass converged independently on both CRITICALs. Separately, found while verifying (not separately reported): **PR #5 is MERGED** (`3faa627`) — enquiries/proposals/approvals and all its CI fixes are on main. **PR #10 is MERGED** (`47298f4`, head `92679c4` after a rebase, 1061 tests) — all three UI carry-over PRs are now on main. New findings from PR #10's rebase, all confirmed: the claim-packet severity ternary hid a real defect (DANGER and ALERT both rendered "warning"); Collections' invented 60/30-day thresholds are replaced by the configured collection-rule ladder; `features/hrdc/tone.ts` was deleted in favour of the kit's `SEVERITY_TONE`. New follow-up (g), confirmed directly at `StatusChip.tsx:118` (`onAccent ? ACCENT_TONE : TONE[tone]`): `StatusChip` on an accent `RecordHeader` card always renders `ACCENT_TONE`, ignoring `tone` — so the claim-window severity chip has never actually been visible, even after today's ternary fix; `ui-lists` deliberately wrote no DOM test for this and said why directly in `hrdc.test.tsx`'s own comment, raising it as a ruling request instead. Also: PR #10's screenshots (opened 11:52) predate PR #9's token merge (11:54) and don't reflect the `--primary-solid`/`SELECTED_TINT` changes.

## BLAST 13 Sep 20:5x (+08) — **Correcting this session's own prior claim: "015-017 do not exist" was wrong.** Confirmed by exact timestamp: `git log --format="%ai"` on `5e7c4bc`/`5d0f31c`/`52caf6b` shows 015/016/017 committed to `cloud/migrations` at 19:35:49/19:42:56/19:59:54 on 13 Sep — all before the D-012 review's own findings doc was committed at 20:08:39 (`eb5b43e`). The review's checkout was detached at `826bb52` (014's commit, 19:30:03) and never fetched, so its claim that "`git log --all` finds no 015_/016_/017_ file ever committed" reflected a stale local tree, not the actual repository. This session had already run `gh pr diff 6` in an earlier pass and seen all three files — the error was repeating the review's contradicting claim without cross-checking it against evidence already in hand. Migration 014's two CRITICAL findings remain accurate and independently confirmed; only the "015-017 don't exist" line was wrong. Two active lanes now: `fix-014` (Opus, worktree `trainos-wt/fix-014`, branch `fix/014-review`, shim :5436) fixing 014's CRIT/HIGH findings with pins that fail against the pre-fix SQL, confirmed in progress (an uncommitted edit sitting in the worktree); `codex-review-014-017`'s continuation (worktree `trainos-wt/codex-pass2`, detached HEAD at `52caf6b` — 017's own tip, confirming a correct fetch this time) reviewing 015-017 plus the nineteen earlier pin edits into a separate report. PR #5 confirmed 17 of 18 checks green at merge time (`gh pr checks 5`) — only npm audit (high+) red, and `CI Summary` itself passed, confirming `continue-on-error` kept it from blocking; `Vitest (unit)` took 14m21s exactly, worth its own follow-up. PR #14 (`fix(ci): make the npm audit gate block on what ships`, branch `ci/audit-scope`, open) and PR #15 (`chore(toolchain): vite 7 + vitest 3 (dev-only audit advisories)`, branch `chore/vite7-vitest3`, draft) both confirmed real, both matching their descriptions exactly: PR #14's diff removes `continue-on-error` and switches to `npm audit --omit=dev --audit-level=high`, with the three GHSA ids named in a comment (`GHSA-fx2h-pf6j-xcff` vite, `GHSA-5xrq-8626-4rwp` vitest, `GHSA-82fw-gwwq-j7x9` `@vitest/mocker`/`@vitest/coverage-v8`) and a note that production is clean at high+ except two moderate react-router advisories needing their own major bump; PR #15's latest commit confirms the 15s Radix-menu test timeout fix is in progress. Merge order, confirmed: PR #6 merges only after BOTH the 014 fix and the 015-017 review land clean; PR #11 (018) merges only after its own separate Codex review. Hosted apply stays gated on PR #6's eventual MERGE verdict and R-F, whichever lands last.

## BLAST 13 Sep 21:0x (+08) — 014's file staying on `main` via PR #12 is intentional (nothing applies to hosted without the user's hand; PR #6's eventual merge supersedes the copy). New rule, already told to the review lanes: a review-report branch is cut from `main` with exactly one doc file, never from the PR under review — cutting from the PR's own tip is what caused the earlier stale-checkout error, since the review worktree then has no reason to fetch further pushes. PR #14 (`ci/audit-scope`) confirmed MERGED (was reported "open awaiting checks" minutes earlier — real time passing, not a wrong report): `npm audit --omit=dev --audit-level=high` now genuinely passes on main. Main is red on exactly one job now, Grant Hygiene, confirmed live: `test_014_..._sql:513`'s `SECURITY DEFINER` function lives on `main` via PR #12 (see the earlier note on 014's file staying there) and fails the same `check:grants` rule it always did; the fix arrives with `fix-014`. New lane `fix-approval-hash` (Sonnet, worktree `trainos-wt/fix-approval-hash`, branch `fix/approval-diff-hash`, not yet pushed to origin) does the client half of 014's HIGH finding #6 — `decideApproval` now sends `p_expected_diff_hash`; the DB-side half stays with `fix-014`. `fix-014` itself hit a real operational problem worth recording: its first shim run collided with a foreign postmaster already holding port 5436, and that first run silently applied 001–017 into the wrong cluster before anyone caught it; the lane now asserts `data_directory` before every run rather than trusting the port number alone. PR #16 (`lane/seeds`, `feat(supabase): fixture world seed, wipe and pin`) confirmed open: fixture world for tenant `akademi-perdana`, 5 commits, 17 files, 9920 total additions (4560 of those in the four `supabase/seeds/*.sql` parts plus the wipe and pin files — team lead's reported "3,848 lines" wasn't reproduced exactly by this count, likely a different exclusion set, not disputed further), generated by a committed `emit-seed.ts` generator with a `--check` flag CI can use to prove the generator and the emitted SQL agree. On the shim: seed exit 0, pin passes, wipe leaves 0 fixture rows, re-seed passes, `lint:sql` 45/45 — all as reported, not independently re-run (needs the shim). Idempotence proof confirmed as a real, stronger-than-usual technique: `pg_stat_xact_all_tables` inside the same re-seed transaction reports 0 inserted/updated/deleted across 98 tables. Three rulings recorded: no single-file runner (because `lint:sql` rejects `\i`, so run order lives in part headers and the README); the 29%-below-floor margin case has no column anywhere in 001–013, logged as a schema gap with a pin that deliberately fails the day a column appears; and the fixture world's 13 action policies collide with provisioning's 22 under a frozen `action_type`, left unresolved pending the user's ruling on which scheme wins. 22 schema gaps total, enumerated in PR #16's own body, now tracked as a SUPABASE SCHEMA sub-item pointing at that PR. PR #13 (`ui/knowledge-tone-rename`) confirmed open but currently failing Grant Hygiene and npm audit — both inherited main-red, not defects in its own diff. PR #15 stays DRAFT, explicitly not for merge.

## BLAST 13 Sep 21:1x (+08) — PR #13 confirmed MERGED at `47d56e1`; its own CI run still shows Grant Hygiene and npm audit as `fail` (checked directly), so it merged carrying the two inherited main-red failures, not because they resolved first. PR #16 (seeds) now retargets 001–017 via a 6th commit and adds T10, a pin that impersonates Alex Selvarajah's JWT and counts landmark rows back THROUGH 014's live RLS policies rather than as the superuser loader — every count confirmed verbatim in the pin's own literal: 6 organisations, 10 engagements, 136 participants, 78 certificates, 10 invoices, 7 approval requests, 16 pipeline steps; the same query as a different tenant's member reads 0 organisations and 0 participants, also confirmed verbatim. `cloud/migrations` resumed at the seeds lane's request and pushed `564dd64`: `app.provision_tenant` gains `p_id uuid DEFAULT NULL`, with an explicit `DROP FUNCTION IF EXISTS app.provision_tenant(text,text,text)` in front of the new `CREATE OR REPLACE` — confirmed mandatory, not tidiness, via the migration's own comment: a bare `CREATE OR REPLACE` with a changed parameter list creates an overload rather than replacing the function, and every existing caller including the pack's own test would then fail with "is not unique" — measured on the shim before the line was written, per the comment. **Correction to a ruling as reported: the seed does NOT invent an HRD-TDF expiry and reference for the four accredited trainers.** Confirmed the opposite directly in the file: all four get `hrd_tdf = false`, `hrd_tdf_valid_to = null`, `hrd_tdf_ref = null` — even though three are TDF-accredited in the underlying fixture — because 017's CHECK constraint refuses `hrd_tdf = true` without a valid expiry, and the file's own comment says plainly: "A wrong boolean that the pin asserts and the PR names beats a fabricated expiry." The seed chose to record a known-wrong value over inventing an audit-actionable compliance date; that's the opposite direction from what was reported. Pipeline step ids are confirmed deterministic (`uuidFor(childKey('pipeline:'+object, "step", stage.key))`), close to but not literally the reported `(tenant_id, 'pipeline:'||stage_key)` formula; the claim that 018 computes an identical formula could not be confirmed, since 018's own pipeline-stage seed is still an open follow-up with no code yet to compare. New USER DECISION queued, not resolved: SST treatment of the three fixture quotations, currently `STANDARD_RATED` at 0%, against Malaysian training often being exempt under the seeded Education Act policy. Two more open items from PR #16, not yet resolved: seed check-keys overwriting two provisioning positions, and the action-policy scheme collision already tracked. `fix-pr5`'s diagnosis of the three slow Radix-menu tests confirmed precise, not a guess: measured 13ms DOM latency and 1ms for a synchronous `getByRole`, with the 6–9s actually spent inside `findBy*`'s `asyncAct` wrapper while floating-ui schedules position work for the open menu — not Radix, not the UI. The real fix (swap those awaits for a settle plus `getByRole`) is left for its own change since it touches files outside this branch's scope. The 15s timeout guess failed on the runner; `testTimeout` is now `30_000`, stated explicitly as a ceiling for a hung test, not a budget.

## BLAST 13 Sep 21:2x (+08) — **Re-correction, not a new mistake: the trainer HRD-TDF entry corrected last round was itself superseded by a later commit.** Re-checked directly at PR #16's current head `f5aa04a` rather than trusting either report. The commit "call provision_tenant with p_id, keep TDF accreditation true" changed the three genuinely accredited trainers to `hrd_tdf = true` with `hrd_tdf_valid_to` reusing each trainer's own TTT certificate expiry — confirmed in the diff (`hrd_tdf: trainer.hrdTdf`, `hrd_tdf_valid_to: trainer.hrdTdf ? trainer.tttValidTo : null`), and `hrd_tdf_ref` stays NULL, also confirmed. This is a stated convention (the same three trainers hold both accreditations, so the seed reuses one real date rather than inventing a second), not a fabrication — confirmed by three new pins, all present and matching exactly: `T2h` (3 trainers carry hrd_tdf), `T2i` (none lapsed at the fixture clock of 2026-11-14), `T2j` ("good news" framing — fails the day a real independent TDF date arrives, so the reuse convention gets caught and removed rather than silently persisting). Both the previous "false" entry and this "true" entry were correct readings of the commit each one checked; the lesson is checking PR head freshness before restating a finding, not sloppier verification either time. PR #16 confirmed at head `f5aa04a`, 7 commits, still targeting 001–017, merging right after PR #6; one more commit still coming for deterministic pipeline stage ids, kept explicitly unconfirmed per instruction since it's a ruling between two lanes, not yet code on either side. PR #15 confirmed BLOCKED, not merely draft-in-progress: checked directly against a live failing CI run (`34757075746`) — even at the raised `testTimeout: 30_000`, `knowledge.test.tsx`'s "keeps Check and Re-ingest out of every row" and `pipeline.test.tsx`'s "offers every other stage in the card's menu" both failed with `Test timed out in 30000ms` verbatim. (The third test named in the original diagnosis, `RowActionMenu`, was not among the failures on this particular run.) The refined diagnosis — a CPU-bound spin inside `findBy*`'s `asyncAct` while floating-ui schedules position work, fixed by a raw poll plus synchronous `getByRole` rather than any timeout — matches what was already on record, and the fixer's refusal to write that rewrite without first reproducing it is the correct call. Negative result confirmed and now doubly on record: raising the timeout is a dead lever, tried at both 15s and 30s. `npm audit (high+)` does pass on this same run, confirming the toolchain upgrade's audit-clearing purpose works independent of the test-timeout question. `fix-pr5`'s lane is reported shut down, but its worktree (`trainos-wt/fix-pr5`, still on branch `chore/vite7-vitest3`) remains present on disk as of this check — noted as a discrepancy between "lane" and "worktree," not disputed as wrong. PR #17 confirmed open, 3 commits, matching its description exactly: `ApprovalRequest.diffHash` and required `ApprovalDecideRequest.diffHash` in the contract, a new `DIFF_CHANGED` error code confirmed mapped to HTTP 409, `rpcClient.decideApproval` sending `p_expected_diff_hash`, fixture-client enforcement on `APPROVE` only matching 011's own scope, and a new conformance test. 1504 tests confirmed exactly — the PR's own test plan states 1118+189+107+90, which sums to 1504.

## BLAST 13 Sep 21:3x (+08) — PR #17 confirmed MERGED at `e20e1ba`, both lanes genuinely shut down (worktrees gone from disk). Caveat worth carrying forward: the conformance suite's "rpc" side runs through an oracle transport into the fixture client, not a real Postgres, so no test in the repo actually exercises 011's real `RAISE` path for `DIFF_CHANGED` — that is exactly what the first hosted-mode run after 014 applies is for. PR #11 (018) confirmed rebased onto `cloud/migrations` (not `main`), now 30 RPCs, 3 views, 11 helpers, 228 assertions; merges after PR #6, reviewed separately by `codex-review-018`. Five findings confirmed against the diff: (a) 014 and 018 both created the three action-envelope gate wrappers with different arity, `CREATE OR REPLACE` made an overload rather than replacing, and every short call went ambiguous (PGRST203) — 014 keeps its five-argument version, 018 drops its own four-argument one, confirmed via a `DROP FUNCTION` in the diff and the migration's own comment quoting the exact verify-time error. (b) 017 left SST half-wired — `sst_rate`/`sst_reason` are plain columns defaulting to 0/STANDARD_RATED with no trigger — confirmed in the diff, and 018's `put_quotation` now resolves the real rate; the table-level trigger fix is routed to `fix-014` rather than fixed here. (c) `core.provenance.origin` is immutable at the schema level, confirmed, so the contract's origin flip is derived in the read projection. (d) Premise correction confirmed by reading `scripts/check-rpc-contract.mjs` directly: it checks envelope shape, forbidden casts and type source, and never reads `RPC_NAMES` — a missing RPC function is invisible to it and surfaces only as `PGRST202` at runtime; added to the backlog as a real gap. (e) The pipeline stage seed for new tenants is blocked on a genuine architecture decision (doc 01 §9 Q10's `DEAL_CHAIN`/`PACKET` ambiguity, a missing `outcome` column needing an `ALTER` on 004, and a real trigger-ordering trap), not busywork. PR #16 confirmed now at 9 commits (the report's `900995a` is two commits behind current head; the extra commit is confirmed docs-only, its own message stating "every id, every row and every assertion is byte for byte what 900995a emitted"). The pipeline-stage-id rule was corrected by measurement after two real defects were caught before shipping: the original "v5 if uuid-ossp else md5" rule would have split ids between environments depending on extension availability, and a plain `stage_key`-only key collides because `WON` is a step name shared across more than one pipeline (the real unique constraint is on tenant+pipeline+step_key). Final rule, confirmed exactly in the current diff: `md5(tenant_id::text || 'pipeline:' || object)::uuid` for the pipeline row, and the same with `|| ':' || step_key` appended for each step; `uuid-ossp` confirmed not installed anywhere in 001–017; two new pins, `T2k` and `T2l`, confirmed present and matching. Confirmed exactly three trainers are accredited, not four — `TRN-0024` Noora Idris carries `FALSE`/`NULL` across every TTT and TDF field in the current row. **The SST ruling reported as corrected is NOT yet reflected in PR #16's committed diff** — checked `quotationsSql` directly at the current head and it still writes no `sst_rate`/`sst_reason` at all, relying on 017's 0%/STANDARD_RATED defaults, the same state the report says was fixed. Recorded as an open discrepancy rather than assumed resolved; the narrower remaining USER DECISION (whether `akademi-perdana` is Education-Act exempt) stays open regardless of which SST state the seed currently reflects, since that's a policy-row decision, not a seed-code one.

## BLAST 13 Sep 21:4x (+08) — **Second D-012 review pass confirmed via `git show` on branch `review/codex-014-017` (commit `5a5655c`) — this is NOT on main yet and no PR is open for it; do not treat as merged.** Verdict, confirmed exactly against the diff: 014 BLOCK (unchanged), 015 MERGE-WITH-FIXES, 016 BLOCK, 017 BLOCK — "None of the four packs may merge as currently written. 015 is the closest to ready," quoted directly. 015: confirmed exactly two `cron.schedule()` calls and zero outbound-HTTP calls (the safety claim holds); one real gap, `app.reap_jobs_all_tenants` has no overload-count guard in its verify block. 016: forward migration confirmed sound; rollback BLOCKED because its `DELETE FROM core.ref_formats` is unqualified across the whole table, deleting every unallocated row rather than only 016's own — the same defect class that blocked 014. 017: four findings confirmed directly against the diff — (1) CRITICAL, SST silently defaults every quotation to zero tax, no write path calls `app.resolve_tax_policy()`; (2) the claimed `NOT VALID`/`VALIDATE` lock-safety property doesn't exist because the file is one transaction start to finish; (3) an unguarded `VALIDATE` on the evaluation-score constraint will abort on an existing legacy 4.5-point score, which the file's own header admits exists; (4) the rollback drops the statutory PDPA breach register and other consent/tax data while claiming only non-customer data is lost — confirmed false as stated; also confirmed catalog pin T3b never executes, since its fixture selects against an empty table. Pin-edit audit confirmed: 14 hunks across 10 files, all legitimate, none a weakened assertion. `check:grants` ruling confirmed: the `pg_temp` SECURITY DEFINER flag on `test_014:513` is a false positive, fix goes in the guard script not the test. G6 confirmed clean on all 17 migrations — proves internal consistency only, as before. Everything routed to `fix-014`, now covering all four packs, not just 014. **Negative result worth a standing checklist line: two packs in the same PR (014 and 016) shipped rollbacks that destroy state they never created and assert the broken result as correct** — "rollback restores PRIOR state, not empty state" added to the migration-authoring checklist. Separately: the SST discrepancy flagged last round is resolved, not a false report — confirmed the fix (quote at 017's default policy SST-G-TRAINING-8, pinned against the policy row) is queued and applied next by the seeds lane, not yet in PR #16's diff as of `e1dca98`. A scratchpad collision between `lane/seeds` and `lane/rpc-018` (both wrote `pr-body.md` at the shared scratchpad root, briefly leaking PR #11's description into PR #16) is recorded as reported; new fleet convention going forward: lane-specific scratch files live under `<scratchpad>/<lane-name>/`, not the scratchpad root.

## BLAST 13 Sep 21:5x (+08) — ⛔ **GitHub Actions has been unavailable on every branch, including main, since roughly 20:36 — confirmed directly, not taken on the report.** `gh api repos/PARALLELPARADIGMS/alex-project/check-runs/103725755232/annotations` returns the literal message: "The job was not started because recent account payments have failed or your spending limit needs to be increased. Please check the 'Billing & plans' section in your settings." Confirmed independently on the most recent push to main: every job completes in 0-3 seconds carrying that same annotation, most never starting (`0s`). Org billing endpoints need `admin:org`, which the available token lacks — **this needs the user, in GitHub's Billing & plans for PARALLELPARADIGMS.** Likely proximate cause, noted not confirmed: Vitest alone has run ~14 minutes per push across roughly twenty pushes tonight. **Policy in effect until fixed:** remaining merges (#6, #11, #16, any report PRs) proceed on the lane's own local gate output plus an independent review verdict, each logged as "CI unavailable, billing" rather than a CI reference; PR #15 stays draft regardless, for its own unrelated reason. PR #16 confirmed final at head `f8d00fc`, 10 commits, seeds lane shut down, merges right after PR #6. The SST fix flagged unlanded two updates ago has now landed, confirmed directly: `quotationsSql` resolves via `app.resolve_tax_policy()` rather than a literal — matching the stated reasoning that tax-policy ids are `gen_random_uuid()` per database, so a literal could never match one — resolving to `SST-G-TRAINING-8` at 800 bps, `STANDARD_RATED` today. Three new pins confirmed present and matching: `T7d` (stored rate matches the resolver), `T7e` (cited policy id matches), `T7f` (an exempt override fails `quotations_exempt_needs_reason` by name rather than storing a lie). `QUO-2026-0184` spot-checked: `sell_price_sen` confirmed `1850000` in the raw insert; SST and gross are resolved/generated columns rather than literals, but the arithmetic is consistent with the reported 148,000 SST / 1,998,000 gross. Two USER DECISIONs remain open in the PR body, both already tracked: the realised 29% margin has no column (T5 pins the absence on purpose), and the fixture action-policy ids collide with two of provisioning's 22 under a frozen `action_type`. **Correction to this session's own prior finding: the pipeline stage seed itself was NOT blocked — only `DEAL_CHAIN` specifically stays blocked, and the earlier finding overstated the scope.** Confirmed directly at PR #11's new head `fc9550c`: the seed shipped as a fourth AFTER INSERT trigger on `public.tenants` (`trg_tenants_z_seed_pipelines`), deliberately named to sort after 016's ref-format trigger alphabetically — resolving the exact ordering trap this thread previously flagged as a blocker, rather than working around it. Ships with a backfill (36 rows over two tenants, 0 on re-run, per the report) and a new `T30` pin; ids use the md5 expression already on record, with the abandoned v5-if-uuid-ossp branch confirmed dropped and `uuid-ossp` confirmed never created anywhere in 001–017. What remains genuinely blocked, confirmed narrowly: only the `DEAL_CHAIN` pipeline object, because 004's own CHECK constraint still cannot store it — divergence 14.2 in the updated `docs/architecture/09` pins that it still cannot be inserted. PR #11 now confirmed at 7 files exactly (`docs/architecture/09`, the 018 migration, its rollback, `test_008`, `test_009`, `test_014`, `test_018`), head `fc9550c`. Three of those seven confirmed as real fixes: `test_008`/`test_009` now seed `is_default = false` (018 seeds every tenant a real default pipeline now); `test_009`'s engagement-creation query confirmed to have carried an unconstrained cross join over `core.pipelines` — with 018's seed a tenant now owns three pipelines instead of one, so without a predicate the insert wrote three engagements and kept whichever came last, meaning every downstream assertion measured an arbitrary row — now fixed, confirmed via the diff's own comment describing exactly this mechanism. `test_014`'s exact grant count moved `121 → 124`, confirmed via the diff's own comment naming both numbers. `docs/architecture/09` gained a full §14 with ten divergences, confirmed present, including 14.2 (`DEAL_CHAIN`) and 14.10 (`core.me_profile()`, still unbuildable, already tracked). The rollback drops the seed trigger and deliberately keeps already-seeded rows. `lane/rpc-018`'s worktree confirmed shut down; both 018 reviewers re-pinned to `fc9550c`.

## BLAST 13 Sep 22:0x (+08) — PR #18 confirmed MERGED at `4162a4d` (`gh pr view 18`: mergedAt 2026-09-13T12:53:11Z, base `main`, merge parent `62097b1`), adding exactly one file, `docs/reviews/2026-09-13-codex-retrofit-015-017.md` — confirmed the new review-branch rule held this time: cut from `main`, one doc file, nothing to go stale. The second D-012 pass is now formally landed, not merely branch-confirmed as it was two updates ago. Verdicts unchanged from the branch-only version: 015 MERGE-WITH-FIXES, 016 BLOCK, 017 BLOCK. `564dd64` (`provision_tenant`'s `p_id` amendment) confirmed reviewed separately and found sound — quoted directly: "the forward migration and the `564dd64` amendment are both sound and independently verified," with 016's BLOCK resting entirely on its unrelated rollback defect. New finding #19, confirmed exactly against the review doc, from a genuine thermonuclear pass (the skill became available mid-session): 017 adds a third tenant-provisioning trigger, `app.seed_compliance_check_keys`, mirroring 016's pattern almost line-for-line, but `app.provision_tenant`'s completeness guard is never updated to also verify check-keys were seeded — the exact failure mode that guard exists to catch (a tenant that looks provisioned but is missing something) is now unguarded for this table too, found by comparing file structure rather than by running anything. Routed to `fix-014` with a request for a registerable per-pack check, since 018 adds a fourth such trigger and three independently-copied seed-trigger patterns is already one too many. Also confirmed from the same pass: 017's 1,340-line single transaction is flagged as a maintainability/decomposition problem independent of its already-found defects, deferred with a catalog note requested. **The "nineteen" pin-edit figure first reported for the Codex second-pass request is confirmed wrong** — recounted three times against three different bases (most recently against `origin/main` as instructed), same result each time: 14 hunks over 10 files, all legitimate, none a weakened assertion. The report's own likely explanation: `main` already carries 014's content by now, so nothing further exists there to find beyond what crossing from 014's own tip already identified. Corrected in place everywhere this spine had repeated "nineteen" as a live figure rather than a historical report. `codex-review-014-017` (the original lane) stays alive for the re-review once `fix-014` pushes its fix covering all four packs.

## BLAST 13 Sep 22:1x (+08) — PR #20 confirmed MERGED at `c7efb8a` (`gh pr view 20`: mergedAt 2026-09-13T13:08:53Z, base `main`, one file `docs/reviews/2026-09-13-thermo-018.md` — review-branch rule held again), a thermonuclear review of PR #11 (018) at `fc9550c`. **VERDICT: BLOCK**, confirmed verbatim, with severity counts confirmed exactly: 5 Blocker, 5 High, 8 Medium, 6 Low. **Correction, read directly against the doc's own B1–B5/H1–H5/M1–M8 headings rather than the summary paraphrase: the reported "5 blockers" mixed severity levels, and two real Blockers were left out of the report entirely — worth being precise about on a migration line this safety-sensitive.** Only two of the five items named are actually Blockers: B1 (one 4,857-line file holding five near-identical copies of one list engine — confirmed via a diffed 25-line block where only two lines differ between `list_proposals` and `list_quotations`) and B3 (`p_view` declared and silently discarded by three of five list RPCs, confirmed via a concrete failing call: `list_proposals(p_view => 'SV-MINE')` returns every proposal in the tenant). The "opposite pagination bugs" are two separate HIGH findings, H2 and H3 — B1's own text explicitly cross-references them ("has already cost correctness twice, in opposite directions — see H2 and H3") rather than folding them in as blocker-severity itself. H2: `list_enquiries` counts after the cursor clause, so `page.total` shrinks per page. H3: the other four hand back a cursor past the end on an exact-multiple page. `regenerate_proposal_section` enqueuing nothing is H1 (High), confirmed by heading, not a Blocker. The `core.provenance` read without a tenant predicate is M1 (Medium), confirmed NOT reachable today per the doc's own words (`subject_id` is always tenant-scoped upstream) — a materially softer finding than "blocker" implies. The pipeline backfill swallowing a foreign-key violation into a `WARNING` is M5 (Medium), confirmed by heading. **Two real Blockers were not mentioned in the report at all, and B4 specifically needs tracking against this whole migration line**: B4 — 018 moves 014's own exact grant-count pin assertion from `v_grants = 121` to `124` (018 adds three view grants), and applying 001–014 alone now makes 014's own `test_014` **fail** with `"expected 124 ... Found 121"` — confirmed by reading the exact failure text in the doc. 014's pin now only passes once a LATER migration has also landed, which `fix-014`'s eventual fix for 014's own CRITICAL findings must not reintroduce or depend on. B5 — 018 has no `BEGIN`/`COMMIT` transaction wrapper on either the forward migration or its rollback, confirmed via `grep` returning nothing, and confirmed every other migration in 014–017 does wrap; matters because 018's tenant backfill loop can fail partway through with no atomicity to fall back on. Confirmed clean, verbatim from the doc's own "What is right" section: tenant scoping is disciplined (every join tenant-correlated, every read filtered through `app.require_tenant_id()`, M1 the sole non-reachable exception); no error is swallowed into `app.ok` (all 18 exception handlers traced); the rollback is inventory-correct (all 47 created objects dropped, nothing from 001–017 destroyed, the one non-018 drop confirmed signature-qualified and unable to touch 014's five-argument function). Pin-honesty (H5) confirmed exactly: 239 labelled assertions (not 228, which the doc itself notes is actually 014's RLS policy count, cited in error elsewhere); four provably unfalsifiable (a GENERATED-column self-comparison, two-distinct-primary-keys-can-never-be-equal, a code check on an already-excluded path, a fixture filter matching zero rows unconditionally); roughly thirty more pass vacuously because they extract from a response without first asserting success; `get_proposal`/`get_quotation` confirmed never invoked by the pin at all. Negative result for the log, confirmed as reported: a pin's assertion count is not evidence of coverage on its own. Lane `fix-018` (Opus, worktree `trainos-wt/fix-018`, branch `fix/018-review`) confirmed active on `lane/rpc-018` at `fc9550c`; the Codex 018 report folds in when it lands.

## BLAST 13 Sep 22:2x (+08) — PR #21 confirmed MERGED at `3fb8ea8` (35 additions/2 deletions, one file, appended to the thermo-018 doc). **018's verdict is now 6 Blocker, 5 High, 8 Medium, 6 Low**, confirmed exactly in the doc's updated severity table. New Blocker B6, confirmed word-for-word: the 018 pipeline seed is not rollback-reversible — the rollback deliberately keeps seeded `core.pipelines`/`core.pipeline_steps` rows because `core.engagement_step_states`'s composite foreign keys force it to (a good reason), but the consequence is that rollback cannot restore prior state at all: `pipelines_one_default_uq` still rejects a default `ENGAGEMENT` pipeline for every tenant afterward, and the original 008/009 fixtures this PR had to edit stay broken with no supported way back. Pin `R4` claims to assert exactly this survival and doesn't — confirmed directly: its comments say "what stays is the DATA, and R4 asserts exactly that," but R4's actual body only queries `pg_trigger` for 016's trigger and never touches `core.pipelines`/`core.pipeline_steps` — the same overclaiming-comment pattern already found in H1 and H5. Routed to `fix-018`: record the seeded ids, make the rollback delete exactly those rows when unreferenced and refuse loudly otherwise, and make R4 assert the real rows. Process note, recorded as reported (a fleet incident, not git-verifiable): a reviewer's worktree was pruned while still amending and recovered only because the branch had already been pushed — new standing rule, never prune a worktree before its lane confirms shutdown, however idle it looks.

**Separately, `fix-014`'s work is confirmed complete and pushed** to `origin/cloud/migrations` at tip `21ec975` (six commits, rebased onto `564dd64`), documented in PR #6's own body under "Review fixes — migration 014." Confirmed directly, not just from the summary table: CRIT-1 fixed via a new `RESTRICTIVE FOR DELETE` policy, `memberships_no_client_delete USING (false)`, confirmed present under that exact name; CRIT-2 fixed via a new pin file, `test_014_rollback_restores_002_grants.sql`, assertions `R1`–`R4` confirmed present; HIGH-1 fixed for exactly three named tables (`ai_provider_keys`, `run_node_io`, `public_share_tokens`) via a confirmed four-sub-case check (`T12a`–`T12d`), deliberately not extended to the other 110 tables; HIGH-2/HIGH-3 fixed; HIGH-4 deferred to the DB side with a pin written to fail once the (already-merged, PR #17) client fix lands; all MEDs fixed; the `check:grants` `pg_temp` exemption confirmed landed in `scripts/check-grants.mjs`. Three more defects found by executing the fix rather than reading it, confirmed exactly: rolling 014 back while 017 was still applied destroyed three things at once (017's grants, an unstamped-policy gap, a manifest-count mismatch measured at 228 stamped vs. 234 derived) — now refused outright with the required order named in the message; `app.apply_tenant_policies` could silently erase an existing role gate via a bare two-argument call, now refused with a literal `'UNGATE'` escape exercised by six branches in a new `T16`; and `001:232` was found to independently grant `USAGE ON SCHEMA core` to `anon`/`authenticated`/`service_role`, meaning 014's header and rollback had both been wrong about this a third time in CRIT-2's own class, now pinned by `R3`. Validation counts confirmed exactly against PR #6's own table: 18/18 forward apply, 17/17 pins (the post-rollback pin correctly _refuses_ while 014 is applied, counted as a pass), rollback 017→014 4/4 dropping 228 stamped policies, `R1`–`R4` pass, re-apply 4/4, `check:grants` 0 findings, `lint:sql` 52/52, `check:rpc` 4 pass/0 broken.

⛔ **BLOCKER, confirmed verbatim from PR #6's own body: the second required reviewer slot is OWED, not filled.** Codex `gpt-5.6-sol` was dispatched alongside the Opus thermonuclear pass (which independently found and fixed five NEW defects introduced by the fix itself, folded into the confirmed findings above) and came back hard quota-blocked: "usage limit … try again at Sep 14th, 2026 12:29 AM," confirmed as the literal message. No verdict has been substituted; the specifically-uncovered gap is the cross-package consumer trace Codex is best at and a structural pass is worst at, and PR #6 asks for a re-run once quota resets before merging. **Fallback in effect per the user's ruling, Kimi unavailable**: all remaining reviews on this migration line run as Opus thermonuclear plus a security pass, with the Codex slot recorded as owed rather than silently dropped — 014's re-review, 018's (finalizing from its own inspection), and 011–013's. 015–017's own fix work is confirmed still open on `fix-014`, not yet pushed as of this check.

## BLAST 13 Sep 22:3x (+08) — PR #22 confirmed MERGED at `279edc3`, one file (`docs/reviews/2026-09-13-codex-retrofit-018.md`) — an independent review of 018 plus the actual G6 execution the static thermonuclear pass never ran, reviewed at the same tip `fc9550c`. **VERDICT: BLOCK, agreeing with the thermo report; all 6 Blockers independently confirmed** with fresh `fc9550c` citations rather than trusted from the earlier report — headings confirmed present and marked "CONFIRMED" for all of B1 through B6. G6 confirmed genuinely run this time: 18/18 pins pass on a full reset-and-apply, and pass again through a rollback→reapply→pin cycle, twice — **confirmed explicitly, worth stating plainly: this does NOT clear B1, B3, H2, H3 or H4**, quoted directly: "the pin suite's fixtures are too narrow to have ever caught B1/B3/H2/H3/H4 on their own." Two genuine nuances found by executing rather than only reading, both confirmed exactly and both worth keeping precise rather than simplified: on **B4**, reproducing the claimed standalone `test_014` failure found it actually fails one assertion earlier than originally cited — at `T1a`, `v_pairs = 116` — and tracing that number directly against `origin/cloud/migrations` at 018's own base commit (before 018 touches anything) found the "116" figure already present, meaning this specific failure predates 018 by one full pack, introduced by 017's own amendment pass and recorded in that pack's own catalog history. This lowers the "018 introduced a new defect" framing but does NOT clear B4: `test_014` genuinely isn't self-contained after either 017 or 018, and 018 separately pushed the grant-count assertion further (121→124) into a file it doesn't own, when it had the option of asserting its own three-grant delta inside `test_018` instead — an older defect existing and 018 making the same class of problem worse are both true at once. On **B6**, the reviewer independently ran the actual rollback scenario rather than only reading it — inserted a real tenant, confirmed the trigger seeds 2 pipelines/16 steps, ran the rollback, confirmed the rows survive while the trigger and functions are dropped, confirmed a post-rollback tenant correctly gets zero pipelines. The data-preservation design itself is confirmed empirically correct, not a design defect — keeping FK-referenced rows on rollback is the right call. The defect stands regardless: R4 checks the wrong trigger (016's ref-format one, unrelated to the pipeline seed) and would not fire if a future edit broke the row-preservation behavior it claims to guard — today's behavior is correct, tomorrow's safety net is checking the wrong object. Codex slot confirmed still owed until 14 Sep 00:29, with named priorities for the re-run, confirmed exactly: the pipeline-seed trigger/backfill/id-collision safety, and the ten client-derived RPCs against `apps/web/src/shared/api/rpcClient.ts` — the doc states plainly neither static pass attempted either. `codex-review-018`'s worktree confirmed shut down.

## BLAST 13 Sep 22:4x (+08) — PR #23 confirmed MERGED at `db0ec94`, one file (`docs/reviews/2026-09-13-codex-retrofit-014-rereview.md`) — an Opus thermonuclear + security re-review of 014's own fix commits, with G6 comparing OLD (`826bb52`) and NEW (`21ec975`) SQL directly. **VERDICT: MERGE-WITH-FIXES.** Both original CRITICALs confirmed genuinely closed by execution, not just by reading the diff: G6 ran the actual DELETE attack against both databases (succeeds on OLD, refused `42501` on NEW), and the rollback reproduces migration 002's five-table grant set verbatim, confirmed through a full round trip including a 017-applied refusal. New HIGH confirmed: `run:read` governs seven `core` tables in migration 013 (`runs, run_nodes, run_node_io, run_events, run_state_cards, run_checkpoints, run_snapshots`), and this fix pack gates only one — `run_state_cards` is worse than the gated table since its own 013 header concedes it skips the 30-day redaction sweep. Not a regression (the gap existed at `826bb52` too), but the catalog's "deliberate posture" framing is a provenance argument, not a security one. Two defects found only by running the pin, not reading it: T11a is a tautology (it `REVOKE`s DELETE then asserts the absence of that same privilege one statement later, so it still passes against the original defective database — only T11b actually catches CRIT-1), and the pin's own header contradicts its own assertion counts (claims "run against 001-014," but its own line 235/258 counts only a 001-017 database produces, confirmed by G6 failing immediately when run against a clean 001-014-only stack as instructed). A separate thermonuclear-lens pass corroborates from a different angle: F5 independently found the same gate-detection gap (`polqual`-only, never `polwithcheck`) the security pass's N-11 already named. Codex `gpt-5.6-sol` still hard quota-blocked until 00:29, recorded as owed, not substituted. Could-not-verify, confirmed stated in the doc's own words: migrations 003/005/006/008/009/010/012 not re-verified beyond what 014 touches, so other `run:read`-style mismatches may exist elsewhere; frontend/worker consumers of the changed grant/policy shapes not traced (Codex's specific brief, still not run).

Separately, `fix-014` confirmed pushed 015–017's fixes to `origin/cloud/migrations` at `bdd49aa` (not yet opened as a PR to main). **015 MERGE-WITH-FIXES, 016 and 017 BLOCK, closed**, confirmed against the commit body: 015 gets an explicit `DROP FUNCTION` before `CREATE`, an exact overload assertion, and a `to_regprocedure` check (T6) — premise corrected, confirmed word-for-word: the finding said the trap "fails invisibly," but 015's existing command check already aborted for a different, misleading reason, and the real exposure is a LATER migration adding the overload. 016's rollback `DELETE` is now scoped to rows matching its own `pg_trigger` derivation rather than the whole table, confirmed end-to-end that a hand-configured format survives a real rollback; a preflight now refuses rolling 016 back while 017 is applied. 017 closes its CRITICAL (the SST default-zero-tax trap is replaced by a BEFORE trigger plus a guarded backfill that refuses rather than guesses for pre-policy quotations, T11 pins six behaviors), corrects the false VALIDATE lock-safety claim without splitting the file (recorded as owed), guards the previously-unguarded `evaluation_responses_overall_score_range` VALIDATE, and corrects the rollback's false "none is customer data" claim with a guard counting the PDPA register, retention policies, consent purpose and SST columns. Finding #19 closed via a new `app.tenant_seed_checks` registry consulted by the provisioning completeness guard; finding #5 closed via RLS assertions on the three new tables. Second premise correction, confirmed word-for-word: the post-rollback policy count is 228→234→228, not `<> 222` as the original review computed. **Deliberately left unfixed and now a pending decision for the user**: 016's hardcoded `dated` ref-prefix flags (`OPP`, `FUP`, `ENG`, `SES` among others) conflict with `docs/architecture/01-domain-model.md` on which entity types can derive a date; since refs are immutable once allocated, a wrong value here is a permanent per-tenant defect from day one — the original review itself called this "not independently confirmed by the security pass" and worth a manual re-check, so `fix-014` routed it to a human ruling rather than guessing, added to `ai/state.md`'s Backlog. Validation counts confirmed exactly: 18/18 forward apply, 17/17 pins pass twice (once before, once after a clean rollback/reapply), `lint:sql` 52/52, `check:grants` 0, `test_014`'s own grant-count assertion unchanged at 121 (matching PR #23's own G6 citations above). Re-review of 015–017 against `bdd49aa` reported dispatched, not yet independently confirmed by this thread.

## BLAST 13 Sep 22:5x (+08) — PR #24 confirmed MERGED at `b9bca03`, one file (`docs/reviews/2026-09-13-codex-retrofit-011-013.md`) — an Opus thermonuclear + security review of migrations 011–013 at commit `e20e1ba`. **011–013 confirmed already on `main`** (their SQL files exist there directly, unlike 014–018 which sit behind PRs), though nothing has been applied to any hosted project. **VERDICT: BLOCK.** **Severity count corrected against the findings table itself, not the doc's own headline framing: 3 CRIT, 7 HIGH, 13 MED, 5 LOW (28 rows total), not the "2 CRIT, 4 HIGH, 9 MED, 5 LOW" first reported.** The verdict's prose headlines only two CRITs as "confirmed by live execution," accurate on its own terms, but a third row (T1) is independently CRIT-severity in the same table and was omitted from the summary entirely: `app.replay_dead_letter` copies `effect_id` onto a replacement job after the original dead-lettering already marked the effect `DEAD_LETTERED`, so a _successfully replayed_ invoice push hits a silent no-op guard in `report_effect_result` and stays recorded as permanently failed with no way to correct it later — static-only (not independently executed this pass), but genuinely CRIT, not a lesser severity just because execution didn't confirm it. The two CRITs that were live-reproduced, confirmed exactly: nothing outside the test pin calls `app.enqueue_effect_jobs` (confirmed by a repo-wide grep), so every external effect (email/invoice/reminder/broadcast) dispatches and never sends; BYOK key rotation is permanently blocked on any key ever revealed (reproduced live: set→reveal→rotate raises `42501 REVEAL_AUDIT_REQUIRED` forever, because the reveal-audit trigger's short-circuit doesn't recognize a rotate's timestamp→NULL transition as distinct from a bare reveal). Neither is exercised by the existing pins, so "all pins pass" does not contradict either finding. HIGH-1 confirmed exactly: `app.has_permission` is called exactly once in all of 011 (inside `decide_approval`); on the HUMAN path, if no `core.action_policies` row matches, dispatch falls through to bare `EXECUTING` with no permission check at all, reachable today for several action types with no policy row. Six more HIGH findings not in the original summary, confirmed present in the table: a worker heartbeat that shortens rather than extends the effective lease (risking a double-send under load); `bulk_decide`'s response shape not matching what the web contract expects (`results` silently `undefined`); an approval diff-hash guard hashing only immutable columns, so `DIFF_CHANGED` can mathematically never fire; unconfirmable `service_role` execute-grant exposure on the BYOK functions (this harness doesn't model Supabase's platform bootstrap); every 013 authorization refusal raising the wrong error-code class, rendering as a session expiry to the web client instead of a permission refusal; and an idempotency hash omitting confidence/reasoning/evidence, letting a downgraded-confidence retry silently inherit the original high-autonomy execution. Catalog/pin-honesty findings confirmed live rather than trusted: the 012/013 pins genuinely require 014's grants applied, contrary to the catalog's "001–013" claim, confirmed by running `test_012`/`test_013` against 001–013 alone first (both failed for exactly this reason) before passing cleanly once 014 was applied — the same pattern already found in 014's own pin. Confirmed clean and worth keeping on record: the 011/014 envelope seam is a genuine, correctly-implemented design, not a catalog oversight; 013's BYOK secrecy mechanism holds (no raw key material ever returned, the reveal ceiling is a real UPDATE predicate); all `search_path=''` pins hold exactly across all three packs. Could-not-verify, stated in the doc's own words: the FORCE-RLS/definer degradation direction is unmeasurable locally (the harness superuser bypasses RLS); `app.aal2_verified` against a real GoTrue-managed `auth.sessions` could not be confirmed against the hand-built stub. Codex still owed until 14 Sep 00:29, not substituted; the two Opus passes ran independently and were disjoint on 30 of 32 raw findings, confirmed exactly. Fixes reported routed to `fix-014` as in-place amendments on `cloud/migrations`, then re-review — not yet independently confirmed by this thread. 001–013 confirmed applied to no hosted project, consistent with every prior check this session.

## BLAST 13 Sep 23:0x (+08) — `fix-014` pushed `ff01f2b` to `origin/cloud/migrations` (not yet a PR), folding in PR #23's 014 re-review items. **N-1 closed in full, confirmed against the migration's own `VALUES` list**: all seven `run:read`-governed tables (`runs`, `run_nodes`, `run_node_io`, `run_events`, `run_state_cards`, `run_checkpoints`, `run_snapshots`) are now gated, not just `run_node_io` — nine tables across three permissions total, matching the commit's own count. The catalog no longer calls the ungated remainder a deliberate posture; confirmed word-for-word it now says "the rest is a gap, not a posture." **T11a's tautology closed, confirmed exactly**: the assertion now reads the shipped privilege set into a variable before any GRANT/REVOKE in the file runs, rather than checking state after the pin's own probe already mutated it — closes precisely the false-pass PR #23 found (T11a passing even against the unpatched database). Pin header range corrected: now states why the migration header's 001–014 figures and the pin's own 001–017 figures are both right rather than contradicting each other. Structural fixes confirmed: policy-comment stamping now takes `p_migration` as a required argument (closes F1's split-ownership finding, 017 now passes `'017'` explicitly); the `'UNGATE'` magic string is replaced by a named function, `app.ungate_tenant_policy()` (closes F3); gate detection now reads both `polqual` and `polwithcheck` (closes F5); T16 exercises six branches. **Negative result worth keeping verbatim: "two dead escapes on one control in one night."** `app.ungate_tenant_policy()`'s first version had the same class of bug as the string it replaced — it called back into the very refusal it exists to bypass, dead on arrival, found only by running it; fixed by dropping the gated policy first. **HIGH-4 closed on both sides, confirmed exactly**: PR #17 (already on main) made the client always send `p_expected_diff_hash`; `core.decide_approval` now refuses an APPROVE with no hash at all (T15c flipped from asserting the old defect to asserting the refusal), and T15d confirms a REJECT without a hash still works, so the fix didn't overcorrect. N-9 closed: `apply_tenant_policies` now refuses a permission every role holds rather than only checking it exists. N-8 narrowed rather than fixed, confirmed stated as such: the share-token gate restores only the role half of 002's intent; the scope half needs an owner column 007 never gave the table, a schema change stated with an owner rather than papered over. Correction confirmed as a crossed message, not new spine information: 015–017's fix slices were already recorded landed in `bdd49aa` two updates ago. Validation counts confirmed exactly: 18/18 forward apply, 17/17 pins pass, rollback 017→014 clean, R1–R4 pass, re-apply clean, 17/17 pins again, nine gated tables measured, `lint:sql` 52/52, `check:grants` 0, `check:rpc` 4 pass/0 broken, `test_014`'s grant-count assertion confirmed unchanged at 121. Next on `fix-014`, reported: the 011–013 amendments (PR #24's findings) — not yet independently confirmed by this thread.

## BLAST 13 Sep 23:1x (+08) — `fix-018` pushed `5612e65` to `origin/lane/rpc-018` (PR #11's own body, "Review fixes — thermonuclear BLOCK cleared"), seven commits closing every finding from PR #20's thermo review with a pin confirmed to fail against the pre-fix SQL before it passes. **B1/H2/H3 fixed (T31)**: the five list RPCs' opposite pagination bugs closed via two shared helpers all five now call; confirmed via the quoted pre-fix failure (`page.total shrank across pages: 4 then 2`). A second, real defect found only by the pin: `list_follow_ups` used bare column names against a three-way join sharing column names, so any paged/filtered call raised an ambiguous-column 500, latent only because nothing had ever paged that list. **B3 fixed (T32)**: confirmed via the quoted pre-fix probe showing every tenant's proposals returned silently on an unresolvable view, with nothing in `appliedFilters` to say so; `list_enquiries`/`list_approvals` gained the same object gate they'd lacked. **H1 fixed (T33)**: `regenerate_proposal_section` now actually enqueues via 012's event path (011's action envelope has no action type for this and 018 may not add one without editing 001–017); `REGENERATE_NOT_ROUTED` refuses if the enqueue didn't land. **M1 fixed (T34)**, plus a new finding filed elsewhere: `core.provenance`'s subject-table allowlist never included `approval_requests`, so the §17 jury badge is unreachable on any database as shipped — not 018's to fix. **M5 fixed (T35)**: the backfill now raises once, naming every tenant it couldn't seed, instead of warning and reporting success. **B2/B5 fixed**: same-commit catalog row, transaction wrapper on both forward and rollback. **H5/M8 fixed**: four provably-unfalsifiable assertions replaced with what each was reaching for. **H4 fixed then superseded**: `cloud/migrations`'s 017 has since grown its own tax-resolution trigger, so 018's check is now "does the trigger exist," not a version check. M2 and M7 explicitly deferred (need a 002 change; a refactor, not a defect fix), not silently dropped. **M4 measured with a number attached, confirmed exactly against the PR body's own table: applying 018 on top of `cloud/migrations`'s CURRENT 014–017 turns FOUR pins red, not two** — `test_008`/`test_009` already amended here (against older versions), but `test_016`/`test_017` are newly affected and outside this branch's scope, both failing on `pipelines_one_default_uq`. **RULING, recorded as a decision only — no `019_*` file exists anywhere in the repo as of this check**: the per-tenant pipeline seed leaves 018 and becomes its own pack, `019_pipeline_provisioning` (trigger, backfill, registry row, B6's rollback contract, the four fixture amendments), same PR #11 lane going forward. **B4 ruling, also a decision only, not yet implemented**: 018 currently moves `test_014`'s SELECT-grant count from 121 to 124, explicitly flagged in the PR body as a rebase-conflict risk against `fix-014`'s already-rewritten `test_014` (which still expects 121); the ruling is that 018 must not edit `test_014` at all, `test_018` should assert its own three-grant delta instead — same "don't edit a file you don't own" principle already applied to 015–017. **Open discrepancy, confirmed present in the PR body's own validation log, not resolved here**: `fix-018`'s own run against `cloud/migrations`'s current 001–017 (no 018 applied) reports `PASS=16 FAIL=2`, naming `test_014` and `test_014_rollback` as already red — timestamped after `ff01f2b`, this thread's own most recent confirmation of "17/17 pins pass." Both are independently true readings of what each lane checked; not yet reconciled. `fix-018` has asked for the exact failing text.

## BLAST 13 Sep 23:2x (+08) — PR #25 confirmed MERGED at `15eed1b`, one file (`docs/reviews/2026-09-13-codex-retrofit-015-017-rereview.md`) — an Opus thermonuclear + security re-review of fix commit `bdd49aa` specifically, **confirmed NOT `ff01f2b`**: the doc states directly that `git diff --stat 21ec975..bdd49aa` touches no `014` file, so 014's own re-review items (the `run:read` gap, T11a's tautology, the pin header mismatch) are explicitly out of scope here and marked pending, consistent with this thread's separate record that those landed in `ff01f2b`. **015 and 016 MERGE-WITH-FIXES, 017 BLOCK (NEW).** 015: the `DROP FUNCTION` guard genuinely works (proven both directions by execution), but the new exact-overload-count verify assertion is dead code — an earlier `to_regproc`-based check in the same block always aborts first with the same misleading message, so the new assertion never runs; the file's own comment also gives backwards advice that would reopen the hazard if followed. 016: the rollback-scope fix is genuinely closed, confirmed by execution on both arms (OLD destroys an operator's format and reports OK; NEW preserves it and deletes only the disclosed residue). Residual: the disclosed residue criterion claims to check `dated` but the actual `WHERE` clause never does, so the disclosed residue is narrower than reality; the new `app.tenant_seed_checks` registry table has no RLS at all, unlike every comparable table in the repo. **017: the CRITICAL SST defect is genuinely fixed (confirmed both arms by direct execution), but the fix itself introduces a NEW blocker: 017 cannot be applied to any database already holding a quotation row.** The new SST backfill's UPDATE queues two DEFERRABLE constraint triggers from migration 007, and the file's own new SET NOT NULL then refuses with SQLSTATE 55006 while those triggers are pending, inside the same one-transaction file — reproduced twice, breaking exactly the "retrofit onto a non-empty database" case 017's own header claims to support. A second, pre-existing, unrelated wall sits behind it: a `margin_rate` scale guard checking a GENERATED numeric-division column that always has scale 20, firing on every non-null quotation unconditionally — present since before this fix, not this fix's fault, but blocking the same scenario. N-1 (HIGH) confirmed: the PDPA rollback guard's quotation sentinel is now permanently tripped by this same fix (sst_reason is NOT NULL on every row), so 015/016/017 have no rollback path at all on any database holding a single quotation. N-2 (HIGH) confirmed: the same false-header-claim class that hid the original CRIT-1 recurs a third time (016's header still says "no table," 017's still says "two new functions" and names no trigger). Both premise corrections re-confirmed by direct execution (015's trap was overstated for a different reason than claimed; 017's policy count is genuinely 234→228). **Negative result worth a standing rule, this thread's own framing but the substance confirmed directly in the doc's words** ("this defect did not exist before this fix, it was introduced by it... specifically the retrofit/non-empty-database case"): a fix that passes clean on an empty shim can still fail on retrofit-onto-existing-data, now true of two separate defects in the same file for the same reason — every backfilling pack needs its own pin that applies over pre-existing rows, not only an empty database. Also confirmed: the new pin's T6 (015) and T7 (016) pass against BOTH old and new code, so they don't actually discriminate the findings they cite even though they're not wrong to have; 017's own T11 is confirmed the one genuinely discriminating regression test in this fix pack. Routed to `fix-014`, Codex still owed. **Operational note, confirmed consistent with this thread's own record of the `core` schema**: the user is on the hosted project's Data API settings page; `core` cannot be exposed there until migration 001 creates it (confirmed at `001:171`), and nothing has been applied to any hosted project yet. Advised to disable "Automatically expose new tables" first, since it would conflict with 014's own narrower explicit grants once 014 lands.

## BLAST 13 Sep 23:3x (+08) — `fix-014` pushed two commits addressing PR #24's 011-013 findings to `origin/cloud/migrations`, tip `0d9e00c` (via `2bac9bf`, not yet a PR). Every fix reproduced live first, each with a pin confirmed to fail against the pre-fix SQL. **CRIT-1 (T2/O2) fixed**: `app.apply_effects` now calls `app.enqueue_effect_jobs`, with an existence check so an 011-without-012 database gets a stated error; T16 confirmed to walk the whole seam — perform, approve, then claim the job as the worker actually would. **CRIT-2 (S1) fixed and confirmed WORSE than the original review found, a genuine severity escalation.** Fixing the reveal-audit trigger's short-circuit exposed a second, independent blocker the review never found: `key_fingerprint` sat in the table's own frozen-column set, so rotation's own fingerprint write raised `IMMUTABLE_COLUMN` for EVERY key, revealed or not — BYOK rotation had never succeeded once, for any key, not merely on previously-revealed ones as the review believed. The frozen list was internally inconsistent (`key_ref`, the other half of "the material changed," was never frozen); now the fingerprint may change only when `key_ref` changes in the same statement, pinned by T14. **T1 fixed, a third CRIT on top of the two already known**: `app.replay_dead_letter` copied `effect_id` onto a replacement job after the original dead-lettering had already moved that effect to `DEAD_LETTERED`, and `report_effect_result` silently no-ops on that status — a genuinely successful replay was recorded as a permanent failure, uncorrectable later. Now reopens the effect to `DISPATCHED` first. **HIGH-1 (T3) fixed, with a real negative result worth keeping precise.** Measured directly: 19 of 22 action types had a policy row, three had none, so the no-policy fall-through was their only path; `app.action_types` gains `required_permission` for all 22, checked immediately for HUMAN/CLIENT actors. The first version of this check ran AFTER payload validation, and the pin caught the leak itself: an unauthorized `SALES` probe of `PAYMENT_RECORD` came back with the action's own payload schema, handing an unauthorized caller information about an action they had no right to attempt — `T17c3` now pins that the fixed version leaks nothing. S5 fixed (all thirteen 013 refusals moved off `42501` onto `TRNOS`, two deliberately kept with the file stating why). S3 fixed, confirmation gap stated rather than claimed: `service_role` is now revoked, but this can't be proven on the local shim (no Supabase default-privilege bootstrap), so hosted confirmation is recorded as owed. S7 fixed: both pin headers now correctly scope the 014 dependency as a dependency of the PIN, not the migration — 012 and 013 themselves apply and verify cleanly with nothing after them. T8 folded in (effect_applier now cleared, closing a real bulk_decide residue risk); catalog corrections folded (011's function count to 28, amendment notes). **Deliberately deferred to backlog, confirmed by absence from both commits' diffs (neither touches `apps/**` or `packages/**`)**: bulk_decide's response-shape mismatch (needs a web+SQL change together), the worker heartbeat lease bug (lives in `apps/worker`), and whether 013 stays without consumers (a product ruling, not a migration fix). **T5's diff-hash guard (`DIFF_CHANGED` can never fire) is routed to `fix-014` now**, confirmed active rather than deferred. Validation counts confirmed exactly: 18/18 forward apply, 17/17 pins pass, rollback 017→014 clean, R1-R4 pass, re-apply clean, 17/17 pins again, `lint:sql` 52/52, `check:grants` 0, `check:rpc` 4 pass/0 broken. Re-review reported dispatched, not yet independently confirmed by this thread.

## Cloud lanes in flight at 15:08 (13 Sep), each on its own branch with a PR to main (superseded by the 19:05 entry above)

- `cloud/opus-pass` LANDED (4fb1ba3): `docs/bd/ai-explorations/2026-09-13-opus-pass.md`. Levy radar drops to rank 5 (no utilisation definition; TrainOS-only claim history under-reads); claim-integrity guard is rank 1; `PROPOSAL_DRAFT` was never a gap, the gap is 9 routing entries for 24 governed types; CHEAP tier sends raw PII to DeepSeek direct (PDPA defect). Merge it.
- `cloud/migrations` running: 013–017 on a self-built PostgreSQL 17 shim; PR "feat(supabase): migrations 013–017". 018 not started (gated).
- `cloud/worker` running: `apps/worker/` Node job worker over the 012 claim RPCs; PR.
- `cloud/routing` running: three-vendor jury, host allow-list, all 24 governed types routed, CHEAP off DeepSeek direct; PR.
- Local `migrations` lane was asked to commit and stop at 15:05; its last HANDOFF entry is the cloud lane's resume point.
- Merge order: routing → worker → migrations (rebase on main if the local lane landed 013 first; keep the higher-assertion version).

## API phase: the executable plan is `ai/briefs/2026-09-13-api-phase-plan.md` (rulings R-A..R-G, eight lanes with prompts, what runs local vs cloud, what needs the user). The section below is the earlier sketch it supersedes.

## Next session: API phase (user's stated goal: get the API contracts up; a few core flows runnable first)

Sequence, with parallel research from the start:

**A. Migrations continue (1 Opus lane, Codex reviewing after quota):** 012 → 013 (carry the twelve enums + two action types) → 014 RLS → 015 → 016 → 017. Then propose 018 "golden path runnable" (enquiry → org match → TNA → programme → proposal + costing with claimable date → approval via policy → send → follow-up → engagement with grant window) with a seed and a walking SQL test; do not start 018 without the user.

**B. Hosted Supabase (user provisions; one gated lane applies):** Supabase Pro, Singapore, per proposal §3.3. Project ref into `supabase/config.toml`. Apply packs to hosted only in a lane that does nothing else, one pack per round, rollback verified on the shim first. Nothing applies before 014 is reviewed.

**C. API layer decision (needs the user):** proposal §3 says .NET modular monolith + Dapper; the repo has no `apps/api`. Options to put to the user with costs: (1) PostgREST + RPCs + Edge Functions behind the contract's 116 endpoint specs, fastest to "core flows up"; (2) .NET API as proposed; (3) thin Node/Hono adapter over Supabase for now. Recommend (1) for the golden path, revisit at Phase 2.

**D. Web off fixtures for the golden path:** swap the `@trainos/fixtures` client for the API client behind the same `useApi`/`useAction` seams, one feature at a time, starting with enquiries → proposals → approvals. 35 files import fixtures today.

**E. Research blast, in parallel from minute one (Sonnet unless noted), each writing one doc under `docs/research/2026-09-<dd>-*.md` and reporting what changed vs the proposal pack:**

1. HRD Corp compliance refresh: Circular 2/2026 text, ACM 2026, eTRIS claim steps, HRD-TDF rules, levy forfeiture/15% deduction, any new circulars since June; diff against `docs/bd/proposal-content-pack-v2.md` §1.5 and Appendix B; propose rule-registry rows.
2. Supabase current docs (Opus): pgvector at scale, RLS + PostgREST performance patterns, Edge Function limits (the 300s slice), pg_cron/pg_net, Vault, branching; what changed since the architecture docs were written.
3. Vector storage decision: pgvector (HNSW/IVFFlat, 1536 vs 3072 dims, filtering by tenant) vs Pinecone vs pgvectorscale, with a cost model at the pack's baseline (40 HRDC packets, corpus tiers A–D) and 10×; when Pinecone becomes necessary, if ever.
4. AI storage practices: embeddings retention, PII redaction before model calls, prompt/response logging under PDPA (amended), per-tenant key isolation, Langfuse self-host; produce a data-handling matrix per tier from proposal §4.1.
5. Model pricing re-baseline: current prices for every tier in §4.1, OpenRouter fees, DeepSeek peak hours vs MYT; recompute §4.2 monthly costs.
6. PDPA amended + MyInvois + SST current requirements affecting schema (retention periods, e-invoice fields, DPO obligations).
7. Read `docs/bd/ai-explorations/README.md` and pick up the eight "for the Opus pass" threads (Opus, later in the session, not first).

**F. Verifier carry-over (`docs/reviews/2026-09-13-verification.md`, gates green at 091e3e0: 1095 tests, build passes, agent runtime proven absent from the production bundle, 63 routes × 4 captures clean):** applied today: strict graduation for `selectNav`, BreadcrumbProvider in the harness, automation modules folded, four trails fixed, three h1s fixed. **Left, ranked:** dark sidebar contrast tokens (close 34 of 40 failing routes); mono-uppercase reduction at kit level; §16 on the enquiry inbox row; ListToolbar on the three enquiry screens and the agent registry; Collections itself non-conformant to §10b; HRD Corp and Invoices nav leaves mount a detail so they have no list; two hand-rolled tables lose zebra; Drawer opening a primary scope; ten tone ternaries; nine missing empty states; profile modal now reads `GET /v1/me/profile` (fab5fe4, `profileDetails.ts` deleted); `useNavSelection` strict nit fixed (7022caa) and `tsconfig.strict.json` now has its first graduate (it had none, so `typecheck:strict` certified nothing); `npm run check:barrels` is a blocking CI job (db9a675). Pipeline board landed (e96435d, 35e59c7; deviations in brief §19a). **Fixture store hands out live objects, so a cached row mutates under React Query; the pipeline feature copies at the boundary; every screen that re-reads after a write has the same exposure — fix in `packages/fixtures` (structuredClone at the client boundary).** The knowledge-sources one-primary conflict is a false positive (modal drawer). **Verifying in a worktree: `node_modules/@trainos/*` symlink into the live packages, so re-point them or you measure the live tree.**

## Research blast E: LANDED (13 Sep 14:15), six docs under `docs/research/2026-09-13-*.md`

- **API layer decision:** `docs/architecture/07-api-layer-decision.md` recommends PostgREST + SQL RPC + Hono functions (Edge or Node); .NET not recoverable this month; first ten endpoints listed; **needs the user's go.** Spec file has 115 rows, not 116.
- **Routed to migrations (fold into 013/014/015/017):** run steps get a redaction-map ref + pii_class; provider keys get zero_retention_enabled + dpa_on_file; routing entries get a host allow-list; `core.perform_action/decide_approval/bulk_decide` wrappers granted to `authenticated` (014); pgvector `hnsw.iterative_scan = relaxed_order` in the retrieval RPC + pin; cron.job_run_details retention job (015); pg_net nudge bounded/batched (012); 5-day query-response rule + HRD-TDF 3-year expiry column; SST default flips to taxable (Group G 8%) with justification when exempt; quotations get SST; contact_consents gets purpose; data_retention_policies + data_breach_register tables.
- **Runtime (API phase):** routing/config.ts disagrees with proposal §4.1 on four of eight tiers; two of three STRONG jurors are Anthropic; Edge Functions have a 2s CPU budget separate from the wall clock and EarlyDrop kills work after the response unless held in waitUntil; Realtime caches RLS per socket.
- **Proposal-pack corrections to make:** HRD-TDF mandate is Circular 6/2024; ACM RM 1,500/h is a late-2024 rate; WhatsApp 1 Oct 2026 change RAISES cost (utility-in-window and service become billable); PDPA DPO/breach/cross-border obligations are in force since 2025, not future; MyInvois Phase 4 mandatory since Jan 2026 (penalty-free to end 2027); Gemini 3.1 Pro price is a fresh GA repricing, re-check; §4.2 AI cost is ~10× too high vs summed token estimates (state conservatively, do not quote RM 11).
- **Open for a human:** HRD Corp publishes no definition of "utilisation" (levy radar depends on it; validate against a live eTRIS account in discovery); two Circular 2/2026 PDFs 404'd; meal allowance outlier source; HRD Corp 7-year retention unconfirmed against a primary circular; Supabase has no managed Malaysia region.
- **Vector storage:** pgvector on Pro through 10 tenants; Pinecone never wins at modelled scale; crossover ~300–500k vectors.
- **Also landed after the wrap call:** fixtures clone at the boundary (e31e802); sidebar contrast outlier fixed (de4ff8f) with a stylesheet-parsing contrast test (33 pairs); verification doc rows 1b/1c open (muted text on selected DataTable row 4.36:1 light; `--on-primary` on solid primary 3.24:1 dark, needs a token split).

## What to eyeball first next session

1. `/sales/pipeline` — the Kanban rebuild (landed at wrap in whatever state pipeline reported).
2. Any page the verification doc flags; the dark sidebar contrast item first, it is one token change closing 34 routes.
3. Profile modal after role switch: job title and email now follow the person (fab5fe4).
